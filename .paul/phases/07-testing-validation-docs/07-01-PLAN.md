---
phase: 07-testing-validation-docs
plan: 01
type: execute
wave: 1
depends_on: [02, 02.1, 03, 04, 05, 06, 06.2]
files_modified:
  - README.md                                          # full rewrite for v0.1 (multi-provider, env setup, Kokoro install)
  - HUMAN_VALIDATION_CHECKLIST.md                      # NEW
  - tests/test_openai_smoke.py                         # NEW (opt-in via OPENAI_SMOKE_TEST=1)
autonomous: true
---

<objective>
## Goal
Close v0.1 with: opt-in real-OpenAI smoke test (cost-bounded), end-user-facing README rewrite covering all 3 v0.1 providers (OpenAI / Ollama discovery-only / Kokoro), human validation checklist for release-day walkthrough.

## Output
- `tests/test_openai_smoke.py`: opt-in `pytest.mark.skipif` on `OPENAI_SMOKE_TEST != "1"`. Sends ~20 chars to `tts-1` model, asserts output file exists + size > 1 KB + valid MP3 header. <$0.01 cost per run.
- `README.md`: rewritten with provider matrix (OpenAI / Ollama / Kokoro), env-var docs (`OPENAI_API_KEY`, `OLLAMA_BASE_URL`, `HF_HOME`, `TTS_MAX_CONCURRENCY`, `OPENAI_SMOKE_TEST`), Kokoro install steps (espeak-ng + pip), troubleshooting section, license note.
- `HUMAN_VALIDATION_CHECKLIST.md`: pre-release walkthrough — install, launch, OpenAI flow, Ollama flow, Kokoro flow (if env set up).
</objective>

<acceptance_criteria>

## AC-1: Opt-in OpenAI smoke test
Skipped by default. Set `OPENAI_SMOKE_TEST=1` + `OPENAI_API_KEY=sk-...` to run. Synthesizes <50 chars. Asserts: MP3 file written, size > 1 KB, starts with valid ID3/MPEG header. Single chunk, single retry (no concurrency).

## AC-2: README rewrite
Replaces outdated single-voice text with provider matrix, env vars, Kokoro install, validation walkthrough link.

## AC-3: HUMAN_VALIDATION_CHECKLIST.md
Step-by-step pre-release walkthrough: install conda env, install ffmpeg, run pytest (266+ pass), launch GUI, OpenAI conversion smoke, Ollama empty-dropdown smoke, Kokoro install + first synthesis.

## AC-4: Regression green
Existing 266 tests stay green. New smoke test counts as +0 in the default suite (skipped). `git diff providers.py settings.py model_discovery.py tts_conversion.py text_processing.py kokoro_synthesis.py main.py combine_and_convert.py` = no changes (Phase 7 is docs + opt-in test only).

</acceptance_criteria>
