---
phase: 05-audio-video-cleanup
plan: 01
type: execute
wave: 1
depends_on: [04-gui-reliability-and-ux]
files_modified:
  - combine_and_convert.py
  - tests/test_combine_and_convert.py
autonomous: true
---

<objective>
## Goal
Audit `combine_and_convert.py` (the MP3-merge + static-image video pipeline). Extract pure-Python helpers (input validation, ffmpeg command builder, GPU encoder probe) so they are testable without instantiating a Tk root or invoking ffmpeg. Lock current behavior with a regression suite. **Decision per PRD §FR-7:** keep the existing direct-ffmpeg approach; do NOT introduce MoviePy. ImageClip/VideoFileClip ambiguity is already resolved via `get_media_height` ffprobe branch.

## Purpose
- Today `combine_and_convert.py` mixes GUI, ffmpeg subprocess calls, and config IO in one file with no tests. A regression here silently breaks the optional video output without anyone noticing.
- The actual encoding pipeline (`is_gpu_encoding_available`, `scale_video`, `get_media_height`, `create_video`) is already direct-ffmpeg-based and reasonably modern. Don't rewrite — extract testable bits.
- Add a `_build_ffmpeg_create_video_command(...)` builder that returns the argv list so tests can assert encoder selection (`h264_nvenc` when GPU, `libx264` when not) without actually invoking ffmpeg.
- Add `_validate_video_inputs(mp3_files, image_file, output_folder, output_filename)` mirroring Phase 4's `_validate_conversion_inputs`. Enumerate missing fields.

## Output
- Refactored `combine_and_convert.py` with extracted helpers + enumerated validation messages.
- New `tests/test_combine_and_convert.py` covering the extracted helpers (no Tk, no real ffmpeg invocation).
- Total tests ≥ 238 (228 + ≥ 10 new).
- SUMMARY.md.
</objective>

<acceptance_criteria>

## AC-1: Extract `_build_ffmpeg_create_video_command(audio_file, image_file, output_file, fps, use_gpu)` → list[str]
Pure-Python; returns the argv list that today's inline call builds. GPU branch → `-c:v h264_nvenc`; CPU branch → `-c:v libx264`. Includes `-tune stillimage`, `-c:a aac`, `-b:a 192k`, `-pix_fmt yuv420p`, `-shortest`, `-r {fps}`, `-y`, `-loop 1`.

## AC-2: Extract `_validate_video_inputs(mp3_files, image_file, output_folder, output_filename)` → (ok, missing)
Returns `(True, [])` when all populated; `(False, [<friendly labels>])` otherwise. Labels: "MP3 Files", "Background Image", "Output Folder", "Output File Name".

## AC-3: Enumerated error message in `start_conversion`
Old: "Please provide all required inputs". New: "Please provide: MP3 Files, Background Image" (joined missing fields).

## AC-4: GPU probe testable via `is_gpu_encoding_available(ffmpeg_runner=None)` injection seam
Accepts an optional `ffmpeg_runner` callable (default: real `subprocess.run`). Tests inject a fake that returns a `CompletedProcess`-shape with `stdout="...h264_nvenc..."` or without.

## AC-5: Tests
`tests/test_combine_and_convert.py` with ≥ 10 tests:
- `TestValidateVideoInputs`: all populated, single empty, all empty, whitespace-only, stable field order.
- `TestBuildFfmpegCommand`: GPU branch contains `h264_nvenc`; CPU branch contains `libx264`; required flags present; fps interpolated; file paths stringified.
- `TestGpuProbe`: fake runner returns stdout with h264_nvenc → True; without → False; raises CalledProcessError → False.

## AC-6: Regression green
≥ 238 tests, exit 0, < 5.0s. No tkinter/ttkbootstrap imports in test file. `git diff providers.py settings.py model_discovery.py tts_conversion.py text_processing.py main.py` returns no changes.

</acceptance_criteria>

<boundaries>
- Do NOT introduce MoviePy.
- Do NOT change ffmpeg flags (audio-quality / video-quality are already-shipped behavior).
- Do NOT modify `providers.py`, `settings.py`, `model_discovery.py`, `tts_conversion.py`, `text_processing.py`, `main.py`.
- Do NOT add new third-party deps.
- Out of scope: a unified GUI that fuses combine_and_convert.py into main.py — separate-window UX is shipped behavior.
</boundaries>
